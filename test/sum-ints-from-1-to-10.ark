(print
  (let [total i]
    (seq
      (prop set (quote total) 0)
      (prop set (quote i) 0)
      (loop
        (seq
          (prop set (quote i) (+ i 1))
          (prop set (quote total) (+ total i))
          (if (= i 10)
            (break total)))))))
