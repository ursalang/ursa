// Ursa prelude.
// © Reuben Thomas 2023
// Released under the MIT license.

{
  // Range iterator.
  range = fn(n) {
    let i = 0
    fn() {
      i := i + 1
      if i <= n { i - 1 } else { null }
    }
  }
}
